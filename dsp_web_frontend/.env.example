# Frontend environment example for strict HTTPS auth flow

# Point to the HTTPS backend to avoid mixed-content issues when the app is served over HTTPS.
# Ensure the backend is running with HTTPS_ENABLE=true on port 3011.
REACT_APP_API_BASE_URL=https://localhost:3011
