# PUBLIC_INTERFACE
# Frontend environment variables for DSP Web UI

# Base URL of the backend API (no trailing slash).
# Example for local development when backend runs on port 3010:
# http://localhost:3010
REACT_APP_API_BASE_URL=http://localhost:3010

# Optional: Brand name shown in the Navbar
REACT_APP_APP_NAME=DSP Web UI
