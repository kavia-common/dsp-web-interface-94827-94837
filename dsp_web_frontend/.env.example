# Frontend environment example

# Point to HTTPS backend to avoid mixed-content when frontend is served over HTTPS.
REACT_APP_API_BASE_URL=https://localhost:3011

# Optional: Application branding
# REACT_APP_APP_NAME=DSP Web UI
